# Environment variables for portfolio deployment
# Copy this file to .env.local and fill in your values

# Optional: If using an email backend service (EmailJS, Resend, etc.)
# NEXT_PUBLIC_EMAIL_SERVICE_ID=your_service_id
# NEXT_PUBLIC_EMAIL_TEMPLATE_ID=your_template_id
# EMAIL_JS_PUBLIC_KEY=your_public_key

# Optional: If using form submission service
# NEXT_PUBLIC_FORM_ENDPOINT=your_endpoint
# FORM_SECRET_KEY=your_secret_key

# Optional: Analytics (Google Analytics, Vercel Analytics, etc.)
# NEXT_PUBLIC_GA_ID=your_google_analytics_id

# Note: Never commit .env.local to version control
# Always keep sensitive keys in .env.local (which is gitignored)
